import { Button } from "antd";

const FullScreen: React.FC = () => {
  return <Button type="text" size="large" icon={<i className="iconfont icon-fullScreen" style={{ fontSize: "24px" }} />} />;
};

export default FullScreen;
