import { Button } from "antd";

const NoticeButton: React.FC = () => {
  return <Button type="text" size="large" icon={<i className="iconfont icon-notice" style={{ fontSize: "24px" }} />} />;
};

export default NoticeButton;
